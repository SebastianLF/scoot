import styled from "styled-components"

const Button = styled.button`
    display: block;
    border: none;
    padding: 1em 2em;
    cursor: pointer;
    border-radius: 5px;
`

export default Button
