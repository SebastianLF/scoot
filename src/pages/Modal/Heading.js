import React from 'react'
import styled from "styled-components"

const Container = styled.h2`margin: 0 auto;`

export default function Heading() {
    return <Container>Add a new project</Container>
}
